# Web API or RestFull Api with swagger integrated sorting of user input array
# Assynconus way of soring the user input array
# If getting any build or runtime error clean the solution and build it again.
 
